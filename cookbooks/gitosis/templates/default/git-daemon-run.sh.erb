#!/bin/sh
exec 2>&1
echo 'git-daemon starting.'
exec chpst -u<%= node[:gitosis][:uid] %>:<%= node[:gitosis][:gid] %> \
  /usr/lib/git-core/git-daemon \
  --reuseaddr \
  --syslog \
  --verbose \
  --base-path=<%= @gitosis_home %>/repositories <%= @gitosis_home %>/repositories
